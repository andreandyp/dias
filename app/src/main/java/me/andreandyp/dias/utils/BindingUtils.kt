package me.andreandyp.dias.utils
/*
import android.widget.TextView
import androidx.databinding.BindingAdapter
import me.andreandyp.dias.R
import me.andreandyp.dias.domain.Alarma

@BindingAdapter("hora")
fun TextView.formatearHora(alarma: Alarma) {
    if(alarma.horas == 0 && alarma.minutos == 0){
        this.text = this.context.getString(R.string.al_amanecer)
        return
    }
    if(alarma.minutos == 0) {
        this.text = this.context.getString(R.string.sonar_a_las, alarma.horas, "00")
        return
    }

    this.text = this.context.getString(R.string.sonar_a_las, alarma.horas, alarma.minutos.toString())
}*/
